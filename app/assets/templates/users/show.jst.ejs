<h3><%= user.escape('name') %></h3>

<img src='<%= user.escape('profile_pic') %>' width="300"><br>

<% if(user.id == FacebookClone.currentUser.id) { %>
  <form id="upload-picture" enctype="multipart/form-data">
    <input type="file" name="user[profile_pic]">
    <input type="submit" value="Set Profile Pic">
  </form>
<% } else { %>
  <% if(user.get('friends')) { %>
    You guys are buddies!
  <% } else if(user.get('requested')) { %>
    Friend Request Pending
  <% } else { %>
    <a class="request-friend" href="">Send Friend Request</a>
  <% } %><br>
<% } %>

Gender: <%= user.escape('gender') %><br>
Email: <%= user.escape('email') %><br>
Birthday: <%= user.escape('birthday') %><br>